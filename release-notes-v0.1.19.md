龙胤立志传 Pro Max 0.1.19

- 重新启用 `卖艺-鉴宝全自动`：`鉴宝` 小游戏里的正确珍宝自动识别/自动纠正逻辑已并回主插件，不再依赖先前被移入 `removed-devonly` 的测试插件。
- 补回探索宝箱 `TreasureChestAutoPickMostValuable` 自动选最高价值奖励逻辑，避免新版源码里配置项仍在、实际行为却失效。
- Electron 启动器新增“自定义天赋”编辑页，可直接维护多条自定义天赋、条件和效果，并写入游戏配置供下次启动生效。
- 新增外部悬浮层配套程序与店铺状态同步通道，为后续店铺购买/状态展示提供独立窗口载体。
- OTA 载荷重新打包，确保 `LongYinStaminaLock.dll`、自定义天赋配置读写、外部悬浮层资源与本次源码一致。

建议这版重点验证：
- `鉴宝` 界面进入后会再次自动锁定正确珍宝，确认提交时也不会选错。
- 探索宝箱多选一事件会自动拿走最高价值物品。
- 启动器里的“自定义天赋”页面可以新增、保存、再次读取配置。
- 更新后 `resources\\payload\\LongYinOverlay` 与 `BepInEx\\plugins\\LongYinStaminaLock.dll` 都随 OTA 一起落地。
