## 主要更新

- 新增藏宝阁容量倍率，默认 10 倍；只调整实时价值上限，不修改物品重量或存档原始数据。
- 新增委托功绩倍率，默认 2 倍；看板声望、NPC 好感与功绩扣除不受影响。
- 新增城市和门派建筑、道路的一键升级，按主楼、特殊建筑、普通建筑、道路依次处理。
- 新增可选的秘籍持续合成；首次确认后自动继续，费用升高、资金或编纂条件不足时停止。
- 功法书拥有状态现在同时检查背包、个人仓库、门派藏书和展览室，并显示最高品质。
- 完善莫高窟掌门管理弟子遗忘武学与天赋的流程；自己保留原版耗时流程，弟子操作直接生效。
- 加强恋人结局成就判定隔离，并避免与外部金钱修改器争用原版金钱方法。

## 下载与 OTA

- 首次安装或手动更新：下载 `LongYinProMaxApp-0.1.50-win-x64.zip`，解压后运行 `LongYinProMax.exe`。
- 应用内 OTA 使用同一 Release 中的 `update-manifest.json`，下载后会校验 ZIP 的 SHA-256。
- 更新会保留 `user-data/**` 与 `BepInEx/config/**`。

## 发布验证

- Windows CI 会重新编译所有维护中的 Mod DLL，并验证源码、暂存 DLL、发布 payload 与 ZIP 内 DLL 的哈希一致性。
- Release 发布后会从 GitHub 的公开下载地址重新下载 ZIP 和 manifest，检查目录安全、版本、哈希与 payload，再运行打包后的 Electron smoke test。
