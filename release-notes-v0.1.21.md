龙胤立志传 Pro Max 0.1.21

- Electron 启动器里的“自定义天赋”现在支持两种触发范围：玩家自身属性达标，以及玩家 + 当前队伍成员的属性总和达标。
- 自定义天赋配置与运行时判定已同步支持队伍总和触发，避免编辑器和插件对同一条条件理解不一致。
- 修复阈值天赋在旧配置或旧运行时标签残留时出现的幽灵条目问题，清理逻辑现在会识别并移除旧阈值标签，避免再冒出列表外的未知天赋。
- 阈值天赋名称在配置被破坏时会回退到默认值 `天人感应`，减少乱码配置导致的异常注册。
- OTA 载荷重新打包，确保启动器内置的 `LongYinStaminaLock.dll` 与本次修复一致。

建议这版重点验证：
- 自定义天赋里选择“队伍属性总和达到门槛”后，玩家加队友的总属性能够正确触发天赋。
- 旧存档或旧运行时残留的阈值天赋不会再继续显示成列表外的未知天赋。
- 重新打开启动器后，`BepInEx/config/codex.longyin.staminalock.cfg` 里的阈值天赋名称仍保持为 `天人感应`。
