龙胤立志传 Pro Max 0.1.18

- 修正 `LongYin Stamina Lock` 的阈值天赋挂载方式，优先通过临时天赋路径 `AddTempTag()` 注入，并补上可见实例检查与循环移除，避免只写入定义却没有稳定英雄天赋条目的情况。
- 阈值天赋默认持续时间改为 `999` 天，方便把这类自动追加的临时天赋当成长驻测试标签使用；属性掉出门槛后插件仍会自动清除。
- 战斗速度记忆修正已随包落实：首次有效战斗速度会被记录，后续战斗会恢复同一节奏，避免冷启动第一场特别快、第二场开始又慢下来的漂移。
- 重新打包 Electron OTA 载荷，确保本次 ZIP 内的 `LongYinStaminaLock.dll` 与当前源码一致，不再出现源码已修正但 OTA 资产仍带旧 DLL 的情况。

建议这版重点验证：
- 阈值天赋达到门槛时会稳定出现在角色天赋/标签表现里，掉出门槛后也会正常消失。
- 冷启动第一场战斗、第二场战斗、手动切一次战斗速度后的后续战斗，节奏保持一致。
- Electron 更新后，直接查看 `BepInEx\plugins\LongYinStaminaLock.dll` 与运行表现一致，不再出现 OTA 版本落后于源码的问题。
