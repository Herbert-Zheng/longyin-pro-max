龙胤立志传 Pro Max 0.1.15

- 新增珍宝铺倒宝助手：进入可交易珍宝的店铺时，界面会读取未鉴定珍宝的潜在卖价，并按实际买卖系数判断利润。
- 新增珍宝自动加购：有利润的未鉴定珍宝会自动加入购物车，但不会替玩家直接结账，方便手动确认。
- 启动器设置页已同步新增 `珍宝自动加入购物车` 开关，保存后会写入 `Commerce -> TreasureAutoTradeEnabled`。
- 去掉了珍宝交易追踪器的热路径日志，保留功能同时减少运行时性能负担。
- 巧手锻造的 `RandomPickUpgrade` 现改为按追加材料的大阶直接增加成品数量，启动器文案也已同步更新。
- 随包插件更新到 `LongYin Stamina Lock v1.28.5`。

建议这版重点验证：
- 启动器设置页能看到并保存 `珍宝自动加入购物车`
- `BepInEx/config/codex.longyin.staminalock.cfg` 中存在 `TreasureAutoTradeEnabled = true/false`
- 进入珍宝铺时，有利润的未鉴定珍宝会自动加入购物车，但不会自动购买
- 关闭该开关后，进入珍宝铺不再自动加购
- 巧手锻造在不同追加材料大阶下，成品数量会按预期变化
