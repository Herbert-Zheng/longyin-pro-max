龙胤立志传 Pro Max 0.1.22

- 修复阈值天赋 `天人感应` 的实例判定，之前会把刚挂上的当前标签误判成缺失，导致每次刷新都反复删除和重挂。
- 现在阈值天赋会正确识别“当前生效标签”，不再在左侧日志里刷屏重复显示 `天人感应 生效`。
- 旧阈值残留清理逻辑仍然保留，会继续移除历史垃圾标签，但不会误伤当前那一个。
- 自定义天赋的玩家属性 / 队伍总和触发能力保持不变。

建议这版重点验证：
- 进入游戏后，`天人感应 生效` 不应再每隔一小会儿重复刷屏。
- 角色面板里只保留一个当前阈值天赋，不再反复消失又出现。
- 之前的自定义天赋队伍总和触发仍能正常工作。
